import { css } from 'styled-components';

export const resetList = () => css`
  list-style: none;
  padding: 0;
`;

export const resetButton = () => css`
  appearance: none;
  background: none;
  border: 0;
  cursor: pointer;
  color: inherit;
`;
